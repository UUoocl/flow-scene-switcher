/**
 * @file Global constants for the Flow Scene Switcher application.
 * @module constants
 */

/**
 * Prefix used for keys when storing flow data in local browser storage.
 */
export const LOCAL_PREFIX = "obs_flow_svelte_";

/**
 * Subfolder within the Obsidian vault where flow files are saved.
 */
export const SAVED_FOLDER = "flow-scene-switcher/saved";
